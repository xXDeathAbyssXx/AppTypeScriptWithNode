<div class="container p-4">
        <div class="row">
            
{{#each Blocks}}
<div class="col-md-4">
    <div class="card">
        <div class="card-header">
            <h3>{{title}}</h3>
        </div>
        <div class="card-body">
            <p>Name: {{name}}</p>
            <span>{{id}}</span>
        </div>
    </div>
</div>
{{/each}}
           
        </div>
    </div>